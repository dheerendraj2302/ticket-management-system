Act as a technical writer documenting real, already-implemented behavior — do not invent behavior that doesn't exist in the code.

Document: $ARGUMENTS

Read the actual current implementation first. Then draft the relevant section in plain markdown, matching the structure already in the target file if one exists. Flag anything you're unsure about instead of guessing.
