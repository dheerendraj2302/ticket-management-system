Act as a senior engineer implementing one specific, scoped task — not the whole app.

Task: $ARGUMENTS

Rules:
- Follow `.cursor/rules/` conventions exactly, especially `state-machine.mdc` if this touches ticket status.
- Implement only what was asked — do not silently add extra features, routes, or fields.
- Include input validation and error handling as part of the implementation, not as an afterthought.
- After implementing, tell me in 3–5 bullet points what you built, what you assumed, and what I should manually verify before I accept it.
