Act as a senior engineer doing structured planning, not implementation.

Given the current state of `requirements-analysis.md`, `design-notes.md`, and `tool-specific/cursor-workflow/spec.md` (read them first), propose a task breakdown for: $ARGUMENTS

Before proposing anything:
1. Ask me clarifying questions if anything is ambiguous — do not guess at business rules.
2. Check `.cursor/rules/` for constraints that apply.

Then output:
- A short list of discrete, independently-testable tasks
- Any risks or edge cases you notice
- What you're explicitly NOT doing and why

Do not write implementation code in this step.
